// Count the total number of characters in a string 
// Count how many vowels are present in a string.
// Count how many consonants are present in a string.

console.log(String.fromCharCode(65))
console.log(String.fromCharCode(90))
let str= "Akarsh";
console.log(str.charCodeAt(0),"letter")
console.log(str[0] <= "A" && str[0]>="Z")

let vowels="aeiou"
// console.log(vowels.includes("a"))
// Count the number of uppercase letters.
// Count the number of lowercase letters.
// Count the number of digits in a string.
// Count the number of spaces in a string.
// Find the first vowel present in a string.
// Find the last vowel present in a string.


// Print each character of a string on a new line.
// Create a new string containing only uppercase letters.
// Create a new string containing only lowercase letters.
// Remove all spaces from a string.
let newstr="a ksgfsd3456knh83et7w";
// console.log(" " <= 9,"hh")
// console.log(newstr.charCodeAt(1),"ascii")
// let count=0;
// console.log(0==="0")
// for(let i=0; i<newstr.length; i++){

//     if(newstr[i]>="0" && newstr[i]<="9"){
//         console.log(newstr[i],"str")
// count++
//     }
// }
// console.log(count,"count")
// i=0;i<=length;
// if(str[i]!==" "){
// newstr=newstr+str[i]
// }
// console.log()
// Extract all digits from a string.
// Replace every space with -.
// Convert all lowercase letters to uppercase without using toUpperCase().


// Convert all uppercase letters to lowercase without using toLowerCase().






// Count how many times a specific character occurs in a string.
// Check whether a string contains at least one digit.
// Check whether a string contains at least one uppercase letter.
// Reverse a string using a single loop.
// Check whether a string is a palindrome.
// Count words in a sentence.





// Find the longest sequence of consecutive spaces.
// Find the total ASCII sum of all characters.
// Print only characters whose ASCII value is even.
// Print only characters whose ASCII value is odd.
// Count special characters (@, #, $, %, etc.).
// Create a new string by taking every alternate character.
// Create a new string by removing all vowels.
// Given "a1b2c3d4", separate letters and digits into two strings.
// Given "JaVaScRiPt", count uppercase and lowercase letters.
// Given "programming", count how many vowels and consonants are present.
// Given "hello123world456", calculate the sum of all digits.
// Given "abcXYZ123", find whether letters or digits are more.
// Given "madam", check palindrome without using built-in reverse methods.
// Given "javascript", print characters at even indexes only.
// Given "javascript", print characters at odd indexes only.
// Given "abc def ghi", count words without using split().
// Given "aabbccddeeff", count total alphabetic characters.

